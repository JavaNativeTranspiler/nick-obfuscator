package dev.name.transformer.transformers.obf.initialization;

import dev.name.transformer.Transformer;
import dev.name.util.java.ClassPool;

public class KeyTransformer extends Transformer {

    @Override
    public String name() {
        return "Key Transformer";
    }

    @Override
    public void transform(ClassPool pool) {

    }
}